[build]
  base = "synergysquad-website"
  command = "npm run build"
  publish = "dist"

# Serve the single-page app for its client-side routes on direct visits.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
